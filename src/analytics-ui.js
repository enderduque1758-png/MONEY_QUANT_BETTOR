(function(){
'use strict';
const $=id=>document.getElementById(id);
const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,Number(v)||0));
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(f))}catch{return f}};
const fmtPct=v=>Number.isFinite(Number(v))?(Number(v)*100).toFixed(1)+'%':'—';
const fmtAge=ms=>{if(!Number.isFinite(ms))return'—';const m=Math.max(0,Math.floor(ms/60000));return m<1?'<1 min':m<60?m+' min':Math.floor(m/60)+' h'};
const FILTER_KEY='mq_chart_filters_v1';
let filters=read(FILTER_KEY,{period:'30',market:'all',rolling:20});

function injectStyles(){if($('modelAnalyticsStyles'))return;const s=document.createElement('style');s.id='modelAnalyticsStyles';s.textContent=`
.model-analytics{margin:7px;padding:10px;border:1px solid rgba(70,210,230,.18);border-radius:9px;background:linear-gradient(180deg,rgba(5,25,33,.92),rgba(3,15,21,.92));color:#58dff7}.analytics-live-head,.model-chart-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:8px}.analytics-live-head strong,.model-chart-head strong{font-size:.72rem;letter-spacing:.08em;color:#e9fbff}.analytics-live-head small,.model-chart-head small{display:block;margin-top:2px;font-size:.58rem;opacity:.65}.analytics-live-head>span,.model-chart-head>span{font-size:.62rem;padding:5px 8px;border-radius:999px;border:1px solid rgba(110,190,210,.22)}.analytics-live-head>span.good{color:#67ff8c;border-color:rgba(103,255,140,.4)}.analytics-live-head>span.warn{color:#ffd166;border-color:rgba(255,209,102,.4)}.analytics-live-head>span.bad{color:#ff7a7a;border-color:rgba(255,122,122,.4)}.quality-grid{display:grid;grid-template-columns:1.35fr repeat(4,1fr);gap:7px}.quality-grid>div{min-height:72px;padding:9px;border:1px solid rgba(90,180,200,.12);border-radius:8px;background:rgba(6,24,31,.7)}.quality-grid small{display:block;font-size:.56rem;opacity:.65}.quality-grid b{display:block;margin-top:5px;font-size:1.02rem;color:#e9fbff}.quality-grid em{display:block;margin-top:3px;font-size:.52rem;font-style:normal;opacity:.56}.quality-score{color:#7dff77}.quality-meter{height:5px;margin-top:8px;border-radius:999px;background:rgba(255,255,255,.08);overflow:hidden}.quality-meter i{display:block;height:100%;width:0;background:currentColor;box-shadow:0 0 10px currentColor;transition:width .35s ease}.quality-alerts{display:grid;gap:5px;margin-top:8px}.quality-alert{display:grid;grid-template-columns:auto 1fr auto;gap:8px;align-items:center;padding:7px 9px;border:1px solid rgba(90,180,200,.14);border-radius:7px;background:rgba(4,18,24,.76);font-size:.58rem}.quality-alert i{width:7px;height:7px;border-radius:50%;background:#66ff86;box-shadow:0 0 8px currentColor}.quality-alert strong{font-size:.6rem;color:#eefcff}.quality-alert span{opacity:.72}.quality-alert.warn{color:#ffd166;border-color:rgba(255,209,102,.25)}.quality-alert.bad{color:#ff7777;border-color:rgba(255,119,119,.3)}.quality-alert.info{color:#6edfff}.quality-alert.ok{color:#66ff86}.quality-legend{display:flex;gap:8px;flex-wrap:wrap;margin-top:7px;font-size:.52rem;opacity:.65}.quality-legend span{padding:3px 6px;border:1px solid rgba(90,180,200,.12);border-radius:999px}.model-chart-head{margin-top:12px}.chart-filters{display:flex;gap:6px;flex-wrap:wrap;align-items:center;margin:7px 0 9px}.chart-filters label{display:flex;align-items:center;gap:5px;font-size:.55rem;opacity:.82}.chart-filters select{height:30px;min-width:90px;padding:0 8px;border:1px solid rgba(80,180,205,.22);border-radius:6px;background:#061820;color:#dffaff;font:inherit}.chart-filter-count{margin-left:auto;font-size:.55rem;opacity:.65}.svg-chart-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.svg-chart-grid article{padding:8px;border:1px solid rgba(90,180,200,.12);border-radius:8px;background:rgba(4,19,25,.72)}.svg-chart-grid header{display:flex;justify-content:space-between;gap:8px;align-items:center;margin-bottom:4px}.svg-chart-grid header b{font-size:.62rem;color:#e9fbff}.svg-chart-grid header span{font-size:.6rem;opacity:.72}.svg-chart{min-height:120px}.svg-chart svg{width:100%;height:120px;display:block}.chart-axis{stroke:rgba(255,255,255,.14);stroke-width:1}.chart-zero{stroke:rgba(255,255,255,.18);stroke-width:1;stroke-dasharray:4 4}.chart-line{stroke:currentColor;stroke-width:2.2;vector-effect:non-scaling-stroke}.chart-dot{fill:currentColor}.chart-empty{height:120px;display:grid;place-items:center;font-size:.58rem;opacity:.5;text-align:center}@media(max-width:900px){.quality-grid{grid-template-columns:repeat(3,1fr)}.quality-score{grid-column:span 2}.svg-chart-grid{grid-template-columns:1fr}.chart-filter-count{width:100%;margin-left:0}}@media(max-width:620px){.quality-grid{grid-template-columns:repeat(2,1fr)}.quality-score{grid-column:1/-1}.analytics-live-head,.model-chart-head{align-items:flex-start;flex-direction:column}.quality-alert{grid-template-columns:auto 1fr}.quality-alert span{grid-column:2}.chart-filters{display:grid;grid-template-columns:1fr 1fr}.chart-filters label{display:grid}.chart-filters select{width:100%}.chart-filter-count{grid-column:1/-1}.svg-chart{min-height:105px}.svg-chart svg{height:105px}}`;
document.head.appendChild(s)}
function allSettled(){return read('mq_history',[]).filter(r=>r&&r.settled&&!r.voided&&Number.isFinite(Number(r.outcome))&&Number.isFinite(Number(r.prob))).sort((a,b)=>new Date(a.kickoff)-new Date(b.kickoff));}
function filteredHistory(){let rows=allSettled();if(filters.period!=='all'){const days=Number(filters.period)||30,cut=Date.now()-days*86400000;rows=rows.filter(r=>new Date(r.kickoff).getTime()>=cut)}if(filters.market!=='all')rows=rows.filter(r=>String(r.market||'h2h')===filters.market);return rows}
function snapshot(){return read('mq_odds_snapshot_v2',null)||{};}
function quality(){const snap=snapshot(),events=Array.isArray(snap.events)?snap.events:[],now=Date.now(),age=snap.at?now-Number(snap.at):Infinity;const fresh=age<=20*60000?1:age<=60*60000?.8:age<=2*3600000?.55:age<=6*3600000?.25:0;let bookTotal=0,withBooks=0,marketTotal=0,soccer=0,soccerContext=0;events.forEach(e=>{const books=Array.isArray(e.bookmakers)?e.bookmakers:[];if(books.length){withBooks++;bookTotal+=books.length}books.forEach(b=>marketTotal+=(b.markets||[]).length);if(String(e.sport_key||'').startsWith('soccer_')){soccer++;if(e.football_prediction&&typeof e.football_prediction==='object')soccerContext++}});const avgBooks=events.length?bookTotal/events.length:0,bookScore=clamp(avgBooks/5),contextScore=soccer?soccerContext/soccer:1;const hist=allSettled(),sampleScore=clamp(hist.length/120),coverage=events.length?withBooks/events.length:0;const score=Math.round((fresh*.30+bookScore*.25+contextScore*.20+sampleScore*.15+coverage*.10)*100);return{score,fresh,age,events:events.length,avgBooks,contextScore,hist:hist.length,coverage,marketTotal,soccer,soccerContext}}
function stateClass(n){return n>=80?'good':n>=55?'warn':'bad'}
function qualityAlerts(q){const out=[];if(!q.events)out.push({level:'bad',title:'Sin eventos cargados',text:'Consulta el calendario para medir la calidad real.'});if(!Number.isFinite(q.age)||q.age>2*3600000)out.push({level:'bad',title:'Cuotas obsoletas',text:'La última sincronización supera 2 horas.'});else if(q.age>60*60000)out.push({level:'warn',title:'Cuotas envejeciendo',text:'Han pasado '+fmtAge(q.age)+' desde la última actualización.'});if(q.events&&q.coverage<.6)out.push({level:'bad',title:'Cobertura de casas baja',text:Math.round(q.coverage*100)+'% de eventos tienen bookmakers.'});else if(q.events&&q.coverage<.8)out.push({level:'warn',title:'Cobertura parcial',text:Math.round(q.coverage*100)+'% de eventos tienen bookmakers.'});if(q.events&&q.avgBooks<1.5)out.push({level:'bad',title:'Pocas casas por evento',text:'Promedio '+q.avgBooks.toFixed(1)+'; aumenta región/cobertura si es posible.'});else if(q.events&&q.avgBooks<3)out.push({level:'warn',title:'Consenso limitado',text:'Solo '+q.avgBooks.toFixed(1)+' casas por evento en promedio.'});if(q.soccer>=3&&q.contextScore<.4)out.push({level:'bad',title:'Contexto de fútbol insuficiente',text:'API-FOOTBALL cubre '+Math.round(q.contextScore*100)+'% de los partidos de fútbol.'});else if(q.soccer>=3&&q.contextScore<.7)out.push({level:'warn',title:'Contexto de fútbol parcial',text:'API-FOOTBALL cubre '+Math.round(q.contextScore*100)+'% de los partidos de fútbol.'});if(q.hist<20)out.push({level:'warn',title:'Muestra histórica pequeña',text:'Solo '+q.hist+' picks liquidados; la calibración aún es inestable.'});else if(q.hist<60)out.push({level:'info',title:'Aprendizaje en progreso',text:q.hist+' picks liquidados; la confianza histórica seguirá madurando.'});if(!out.length)out.push({level:'ok',title:'Calidad estable',text:'No se detectan alertas críticas en los datos actuales.'});return out.slice(0,5)}
function mount(){const anchor=$('backtestStrip');if(!anchor||$('modelAnalyticsPanel'))return;const wrap=document.createElement('section');wrap.id='modelAnalyticsPanel';wrap.className='model-analytics';wrap.innerHTML=`<div class="analytics-live-head"><div><strong>LIVE DATA QUALITY</strong><small>Frescura · cobertura · contexto · muestra histórica</small></div><span id="dqState">SIN DATOS</span></div><div class="quality-grid"><div class="quality-score"><small>Calidad global</small><b id="dqScore">—</b><div class="quality-meter"><i id="dqMeter"></i></div></div><div><small>Frescura</small><b id="dqFresh">—</b><em id="dqAge">—</em></div><div><small>Casas / evento</small><b id="dqBooks">—</b><em id="dqCoverage">—</em></div><div><small>Contexto fútbol</small><b id="dqContext">—</b><em>API-FOOTBALL</em></div><div><small>Muestra liquidada</small><b id="dqHistory">—</b><em>autoaprendizaje</em></div></div><div class="quality-alerts" id="dqAlerts"></div><div class="quality-legend"><span>Frescura 30%</span><span>Casas 25%</span><span>Contexto 20%</span><span>Historial 15%</span><span>Cobertura 10%</span></div><div class="model-chart-head"><div><strong>MODEL PERFORMANCE · SVG</strong><small>Resultados liquidados con filtros interactivos</small></div><span id="chartSample">n = 0</span></div><div class="chart-filters"><label>Periodo<select id="chartPeriod"><option value="7">7 días</option><option value="30">30 días</option><option value="90">90 días</option><option value="all">Todo</option></select></label><label>Mercado<select id="chartMarket"><option value="all">Todos</option></select></label><label>Ventana<select id="chartRolling"><option value="10">10 picks</option><option value="20">20 picks</option><option value="30">30 picks</option><option value="50">50 picks</option></select></label><span class="chart-filter-count" id="chartFilterInfo">—</span></div><div class="svg-chart-grid"><article><header><b>Acierto móvil</b><span id="hitLast">—</span></header><div id="hitChart" class="svg-chart"></div></article><article><header><b>Brier móvil</b><span id="brierLast">—</span></header><div id="brierChart" class="svg-chart"></div></article><article><header><b>ROI acumulado</b><span id="roiLast">—</span></header><div id="roiChart" class="svg-chart"></div></article></div>`;anchor.insertAdjacentElement('afterend',wrap);bindFilters();populateMarkets()}
function populateMarkets(){const sel=$('chartMarket');if(!sel)return;const markets=[...new Set(allSettled().map(r=>String(r.market||'h2h')))].sort(),current=filters.market;sel.innerHTML='<option value="all">Todos</option>'+markets.map(m=>'<option value="'+m.replace(/"/g,'&quot;')+'">'+m.replaceAll('_',' ')+'</option>').join('');sel.value=markets.includes(current)?current:'all';if(sel.value!==current){filters.market=sel.value;saveFilters()}}
function saveFilters(){try{localStorage.setItem(FILTER_KEY,JSON.stringify(filters))}catch{}}
function bindFilters(){const period=$('chartPeriod'),market=$('chartMarket'),rolling=$('chartRolling');period.value=String(filters.period||'30');rolling.value=String(filters.rolling||20);period.onchange=()=>{filters.period=period.value;saveFilters();performance()};market.onchange=()=>{filters.market=market.value;saveFilters();performance()};rolling.onchange=()=>{filters.rolling=Number(rolling.value)||20;saveFilters();performance()}}
function rolling(rows,size,fn){return rows.map((_,i)=>{const s=rows.slice(Math.max(0,i-size+1),i+1);return fn(s)})}
function svgLine(values,opt={}){const clean=values.map(v=>Number.isFinite(Number(v))?Number(v):null);if(!clean.some(v=>v!=null))return'<div class="chart-empty">Esperando datos liquidados.</div>';const W=420,H=130,p=18,valid=clean.filter(v=>v!=null),min=opt.min!=null?opt.min:Math.min(...valid),max=opt.max!=null?opt.max:Math.max(...valid),span=Math.max(1e-6,max-min);const pts=clean.map((v,i)=>v==null?null:[p+(W-2*p)*(i/Math.max(1,clean.length-1)),H-p-(H-2*p)*((v-min)/span)]).filter(Boolean),path=pts.map((q,i)=>(i?'L':'M')+q[0].toFixed(1)+' '+q[1].toFixed(1)).join(' '),base=opt.zero&&min<0&&max>0?H-p-(H-2*p)*((0-min)/span):null;return`<svg viewBox="0 0 ${W} ${H}" role="img" aria-label="${opt.label||'Gráfica del modelo'}"><line x1="${p}" y1="${H-p}" x2="${W-p}" y2="${H-p}" class="chart-axis"/>${base!=null?`<line x1="${p}" y1="${base}" x2="${W-p}" y2="${base}" class="chart-zero"/>`:''}<path d="${path}" class="chart-line" fill="none"/><circle cx="${pts.at(-1)[0]}" cy="${pts.at(-1)[1]}" r="3.5" class="chart-dot"/></svg>`}
function performance(){const rows=filteredHistory(),roll=Math.max(5,Number(filters.rolling)||20);$('chartSample').textContent='n = '+rows.length;$('chartFilterInfo').textContent=(filters.period==='all'?'Todo':filters.period+'D')+' · '+(filters.market==='all'?'todos los mercados':filters.market.replaceAll('_',' '))+' · ventana '+roll;if(!rows.length){$('hitChart').innerHTML=$('brierChart').innerHTML=$('roiChart').innerHTML='<div class="chart-empty">Sin resultados para estos filtros.</div>';$('hitLast').textContent=$('brierLast').textContent=$('roiLast').textContent='—';return}const hit=rolling(rows,roll,s=>s.reduce((a,r)=>a+Number(r.outcome),0)/s.length),brier=rolling(rows,roll,s=>s.reduce((a,r)=>a+Math.pow(Number(r.prob)-Number(r.outcome),2),0)/s.length);let eq=0;const roi=rows.map(r=>{const price=Number(r.price),p=Number(r.prob),bet=price>1&&p*price>1;if(bet)eq+=Number(r.outcome)===1?price-1:-1;return eq});$('hitLast').textContent=fmtPct(hit.at(-1));$('brierLast').textContent=Number(brier.at(-1)).toFixed(3);$('roiLast').textContent=(roi.at(-1)>=0?'+':'')+roi.at(-1).toFixed(2)+' u';$('hitChart').innerHTML=svgLine(hit,{min:0,max:1,label:'Acierto móvil de '+roll+' picks'});$('brierChart').innerHTML=svgLine(brier,{min:0,max:.5,label:'Brier móvil de '+roll+' picks'});$('roiChart').innerHTML=svgLine(roi,{zero:true,label:'ROI acumulado en unidades'})}
function renderQuality(){const q=quality();$('dqScore').textContent=q.score+' / 100';$('dqMeter').style.width=q.score+'%';$('dqState').textContent=q.score>=80?'ALTA':q.score>=55?'MEDIA':'BAJA';$('dqState').className=stateClass(q.score);$('dqFresh').textContent=Math.round(q.fresh*100)+'%';$('dqAge').textContent='última cuota '+fmtAge(q.age);$('dqBooks').textContent=q.avgBooks.toFixed(1);$('dqCoverage').textContent=Math.round(q.coverage*100)+'% eventos con casas';$('dqContext').textContent=q.soccer?Math.round(q.contextScore*100)+'%':'N/A';$('dqHistory').textContent=String(q.hist);$('dqAlerts').innerHTML=qualityAlerts(q).map(a=>`<div class="quality-alert ${a.level}"><i></i><strong>${a.title}</strong><span>${a.text}</span></div>`).join('')}
function refresh(){if(!$('modelAnalyticsPanel'))mount();if(!$('modelAnalyticsPanel'))return;populateMarkets();renderQuality();performance()}
function boot(){injectStyles();mount();refresh();setInterval(refresh,15000);window.addEventListener('storage',refresh);document.addEventListener('visibilitychange',()=>{if(!document.hidden)refresh()});const nativeFetch=window.fetch;window.fetch=async function(){const r=await nativeFetch.apply(this,arguments);setTimeout(refresh,250);return r}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
