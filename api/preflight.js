import {persistenceInfo} from '../server/store.js';
function send(res,status,data){res.setHeader('Cache-Control','no-store');res.setHeader('Content-Type','application/json; charset=utf-8');return res.status(status).json(data)}
export default function handler(req,res){if(req.method!=='GET')return send(res,405,{error:'Método no permitido'});const persistence=persistenceInfo(),checks={theOddsApi:Boolean(String(process.env.THE_ODDS_API_KEY||'').trim()),apiFootball:Boolean(String(process.env.API_FOOTBALL_KEY||'').trim()),durableStore:persistence.durable,node22:String(process.version||'').startsWith('v22.')};const missing=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return send(res,missing.length?503:200,{ok:!missing.length,checks,missing,persistence,requiredRoutes:['/','/api/health','/api/sports','/api/odds','/api/state','/api/settle','/api/closing-line'],timestamp:new Date().toISOString()})}
